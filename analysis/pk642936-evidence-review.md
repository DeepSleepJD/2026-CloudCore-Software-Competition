# PK-642936：现有证据复核

用户确认该场使用 v7，提供的资料仅有 pk-642936-analysis.md，并确认对局观测可用、原报告的代码归因错误。没有原始模型回复、沙盒结果和逐回合 HTTP 请求。因此不再以补充日志为分析前提，也不声称已还原六次失败的唯一原因。

## 可采用的对局事实

- 我方六次任务均未完成，对手六次全部完成。
- 按报告时间线，我方领取到失败分别跨 16、16、11、11、11、11 回合；对手领取到提交为 3–4 回合。这是观察间隔，不能单凭它区分超时、离场或错误提交。
- R42 出现答案 `15`。其生成原因未知，不能认定为平台默认提交。
- 前两次失败发生于首夜之前，夜间避险不能解释这两次失败。
- 角色没有移动/动作，不能证明未调用模型或沙盒。回放字段为空也不能直接等同于程序实际收到的请求字段为空。
- 原报告关于 brain.py 没有任务逻辑的结论不适用于 v7；民间传闻中的石门属于宝藏玩法，不能与工程修复/城市查询自进化题混为一谈。
- 下载目录存在 v3.1 旧包，但用户已明确本场使用 v7；不将旧包作为本场失败原因。

## 部署包对照复现

实际解压并载入 CoreGeek-v7.tar.gz 与 CoreGeek-v7.1.tar.gz 的模块，使用参考资料保留的北京题面作为输入。两包源码分别为 bfd233262378dea25fd62ca1efbb1a646d37e988、48aa14259c2cb10ec967be472c0fe24d336045b4。机器结果见同目录 pk642936-package-comparison.json。

| 相同场景 | v7 包 | v7.1 包 |
| --- | --- | --- |
| 读取真实题面的“提交形式”示例 | 未识别答案格式 | 识别 JSON 对象和字段类型 |
| 模拟一次成功命令输出 15，随后模型回答裸数字 15 | 实际生成 submitAnswer，taskAnswer 为字符串 15 | 阻止该格式错误的提交 |
| 查询要求绑定 city 等本题固定字段 | constant 操作不支持 | 可与统计值一起生成答案 |
| 文档分别提供服务地址和接口路径 | 拒绝合法的组合 URL | 接受同源且有明确路径依据的 URL |
| 指定任务文件在当前目录下的动态子目录中 | 直接 cat 相对文件名不能读取 | 实际执行沙盒辅助程序后定位成功，并读到引用的同目录说明 |

裸数字场景的模型回复与命令结果是本机主动构造的输入，不是从该场恢复出的原始记录。它证明 v7 存在与报告现象一致的缺陷路径，不能证明 R42 的数字由同一过程产生。动态目录和 URL 场景同样是缺陷复现，不代表已知该场目录与接口的实际状态。

## 决策

现有证据足以认定 v7 的文档读取、答案格式识别及查询工具存在缺口，可能造成错答和无效往返；v7.1 已针对这些可复现缺口修复。无需等待不存在的日志才承认和处理这些缺陷。

尚不能确定每次失败的具体命令、模型是否误解题意、是否发生鉴权错误、各题是否取得部分分，也不能根据对手 3–4 回合完成推断其隐藏实现。此次只补充对照证据，不修改运行代码、不产生额外参赛版本；现有可交付修复包仍为 v7.1。
