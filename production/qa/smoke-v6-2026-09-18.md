# JD v6 自动冒烟记录

日期：2026-09-18。对象：Python HTTP 比赛程序，无游戏引擎或 UI。已有 unittest 测试目录及 Linux/Windows × Python 3.10/3.12 的 CI；没有独立 sprint QA 计划。依据用户此前要求，自动验证并记录，不要求人工逐条测试。

## 本机结果：PASS

- 完整测试 105 项通过，耗时 23.615 秒。
- 新增 16 项任务回归：正文匹配与实际计算、跨题缓存重新读题和计算、准确的匹配错误反馈、模型/沙盒答案在普通危险区内提交、近身用药保留答案、撤退与跨题清理、暂停时保存命令、错误答案纠正、超过 12 次调用仍可完成、领取计时与截止前提交、排除超时/错误/离开/死亡/任务未结束的缓存确认、命令及网络故障、超时输出不作为答案、旧缓存隔离、长回复协议解析。
- 七项变异检查全部检出，包括故意删除正文、恢复 12 次模型上限、避险时丢弃返回。这些变异只在测试进程内生效。
- 打包集成测试解压生成的 tar.gz，从无关目录启动真实 HTTP 服务；经 HTTP 领取任务、读取题目、请求模型、执行固定测试 solver、提交计算出的 count=23。模型/裁判仍由测试夹具模拟。检查 CoreGeek 根目录、入口、脚本执行位和换行。
- 既有 240 个场景边界组合、两个种子共 520 回合的简化模拟，以及采矿、炮火、送修、夜间策略回归随完整测试通过。
- git diff --check 通过。

## 测试范围

测试执行的是审阅过的固定程序，不运行真实模型返回的未知代码。真实模型和裁判题库未联调，不声明真实任务通过率、比赛胜率或“必然成功”。无手工测试记录；UI/操作手感检查不适用于该 HTTP 程序。

远端 CI 与最终提交包校验结果在发布后补充。
