# JD v4 自动冒烟记录

日期：2026-09-18。范围：基于 feature/jd a7b2b00 的运行代码、五项策略改进、官方目录格式提交包。结论：本机自动检查 PASS；官方完整对局及真实模型正确率未验证。

## 结果

| 检查 | 实测结果 |
| --- | --- |
| 完整 unittest 回归 | 77 项通过，18.711 秒；含旧版对照和新增 JD 测试 |
| 自动边界组合 | 240 个子场景，覆盖两个出生侧、四个时点、五档资金、三种目标归属 |
| 连续回合 | 两个种子各 260 回合，共 520 回合通过；覆盖交易、任务、建造、升级和炮击 |
| 关键变异检查 | 四项故意引入的错误全部检出 |
| 真实回放快照 | 206 个快照、301 条指令检查通过，单次最大 0.0443 秒 |
| 打包部署 | 解压后从其他目录启动 main3.py；三轮含 null 请求均有动作；健康检查无异常 |
| Linux shell 入口 | GitHub Actions 在 Python 3.10、3.12 环境均实际启动成功 |

回放末尾第 1027 行不完整，解析时跳过；因此上述数字不代表全部回放均被验证。

## 需求覆盖

| 需求 | 自动验证内容 |
| --- | --- |
| 敌我识别 | 己方、对方、未知标记、混合波次，以及近身自保例外 |
| 三种任务渠道 | 接任务、模型到沙盒到答题闭环、任务耗尽退出；新闻证据和时间校验；宝藏材料、成功停止及错误停止 |
| 墙体与武器预算 | 墙体紧急维修、先用药、短途送修、维护预留、天亮前维修、多人采购去重 |
| 白天开拓者调度 | 接任务、工人接炮、夜间任务结束后继续守炮、重复请求不额外调用模型 |
| 经济与生存 | 囤货与现金不足分支、停矿、移动和操作合法性、金币不超支、连续回合调度 |
| 部署和稳定性 | 官方包目录、启动脚本权限及换行、真实 HTTP 请求、null 字段、160 机器人性能场景 |

变异检查分别关闭敌我筛选、清空维护预留、禁止开拓者接任务、禁用近身救急。四项均导致对应回归测试失败，证明这些测试能发现目标错误，而不只是执行代码。

## 复现

```bash
python -B -m unittest discover -s tests -q
python -B tools/mutation_checks.py
python -B tools/check_replay.py ../replay.json --stride 10
python -B tools/package_submission.py dist/CoreGeek.tar.gz
```

CI 矩阵：Linux/Windows × Python 3.10/3.12，四组全部通过，并额外在 Linux 启动压缩包内 bash run.sh。[验证记录](https://github.com/DeepSleepJD/2026-CloudCore-Software-Competition/actions/runs/35323720705)，对应提交 `0f6ca3f`。第一轮发现较早的 Windows Python 3.10 不支持测试所用的 tarfile 解压过滤接口，已为自建可信测试包增加兼容分支，复跑通过；比赛运行代码无需修改。

正式提交包的 BUILD.json 指向运行代码提交 `9ca9a8e`，之后仅更改测试及说明。包内 agent 文件已逐个核对与工作区一致。SHA256：`86fd5dd5d907b42b50650107616cafa5dee306344cc7531bd9eaf65e7f6e67e4`。

## 限制

自动测试使用可控模型回复。连续回合测试是独立简化规则模拟，未实现完整机器人 AI、官方建造区域、对手行为和最终积分；不能据此计算胜率或保证真实比赛不会失败。没有以人工回放作为本次交付前提。本项目是 Python HTTP 选手程序，游戏引擎编译和画面检查不适用。
