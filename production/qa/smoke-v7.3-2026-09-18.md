# v7.3 任务调用日志与缓存失效验证

## 范围

Python 标准库服务；已有 unittest、GitHub Actions 矩阵及部署集成测试。本次沿用 smoke-check 的关键路径验证，使用自动测试替代人工反复上传对战。改动集中于任务证据、日志恢复工具、失败缓存落盘；战斗和经济策略没有调整。

## 本机验证：PASS

- 最终完整测试 154 项通过，35.615 秒。此前 152 项完整测试及新增 13 项日志测试也分别通过。
- 13 项新增回归覆盖：连续首题成功/次题失败与状态隔离；实际 prompt、executeCmd 和回复关联；命令超时；任务书与工具参数；威胁撤退；任务无反馈消失；基地/全员消失；重复回合不重复统计；缓存判错后重启及任务直接结束两条路径；文件不可写不影响真实提交；模型网络错误与任务超时区分；中文/换行长日志完整恢复与丢片报告；文件滚动与显式截断。
- 已有部署集成测试增加实际标准输出检查：解压 tar.gz，从不同工作目录启动真实 HTTP 服务，读取任务书、执行固定解法提交 count=23、完整分页查询提交 count=2。恢复 stdout 后，实际命令和两次答案与接口返回一致。
- 14/14 变异检查检出。新增故意去掉发出调用日志、恢复只从内存删除失败解法，测试均失败。
- git diff --check 通过；构建包中 16 个源码/说明文件逐字节校验（统一 LF）与工作区一致。
- 测试使用固定模型回复、受控本地接口和自有辅助代码，没有调用比赛真实模型或复现这场实战。因此 PASS 说明实现和覆盖场景通过，不能证明模型解题成功率。

## 远端验证：PASS

提交 07f192e 的 Linux/Windows × Python 3.10/3.12 四组全部通过：154 项完整测试、14 项变异检查及 tar.gz 构建。两组 Linux 的官方 run.sh 启动及 v7.3-jd 健康检查通过。[执行记录](https://github.com/DeepSleepJD/2026-CloudCore-Software-Competition/actions/runs/35348386328)。

## 提交包

版本 v7.3-jd，源码提交 9ff35b4331fbd572faa718dd497733aaf02a9c68。官方 CoreGeek/ 根目录，run.sh 权限 0755，LF 换行，包含新增 agent/task_trace.py，不含本地日志和缓存。

仓库 artifacts/CoreGeek.tar.gz 与本地 CoreGeek-v7.3.tar.gz 字节一致，37379 字节；SHA256：`df650459dd92ea66338d658f9922003bd442e110b6aa55594deb3f6c61c36854`。
