# JD v7 自动冒烟记录

日期：2026-09-18。对象：Python HTTP 比赛程序。依据用户要求，以自动化测试验证，不要求人工逐条复测。

## 本机结果：PASS

- 最终代码完整回归 124 项通过，耗时 42.285 秒。
- 新增 19 项任务测试：offset/page/cursor 分页、重叠记录去重、重复页、总量变化与漏页、鉴权失败、合法空结果、显式年代顺序、答案字段类型与部分分、错误答案去重、重复命令拦截与正常进展、真实验收 token、修复后自动验收、失败验收不提交、旧请求结果拒绝、未知题与模糊工作目录回退、查询地址依据检查。
- 十项变异检查全部检出：故意删除关键判断后，对应测试均失败。变异只在测试进程内生效。
- 部署集成测试解压官方格式 tar.gz，从无关目录启动真实 HTTP 服务，完成普通任务流程，并通过本地分页接口完成 query→取齐两页→提交 count=2。验证模型调用次数，成功查询与验收无须额外模型往返。
- 既有战斗、经济、送修、夜间策略、240 个场景边界组合与两个种子共 520 回合的简化模拟随完整测试通过。
- git diff --check 通过。

## 测试范围

模型、题目接口和裁判反馈使用可控测试夹具；沙盒测试实际执行本项目固定查询/验收辅助程序。没有连接真实模型或裁判题库，不能据此声明实际任务成功率或比赛胜率。查询工具的完整性检查也不能证明模型选择的统计口径正确。任意 command/skill 仅能识别部分明确失败和分页不完整情况。

本批变更范围和通用流程边界见 [v7 交付说明](../../docs/v7-delivery.md)。提交包及远端 CI 结果在打包、推送后记录于下方。

## 提交包

版本 v7-jd，BUILD.json 源码提交 bfd233262378dea25fd62ca1efbb1a646d37e988。15 个包内源码/说明文件与最终工作区逐一核对一致；CoreGeek 根目录、run.sh 执行位 0755、LF 换行均通过。

本地 CoreGeek-v7.tar.gz 与仓库 artifacts/CoreGeek.tar.gz 字节一致，大小 30634 字节，SHA256：`8bb997c77fcf6116f30d307b9e4612390382cc59fa10b134edcba959bc2be1c1`。

## 远端结果：PASS

提交 e3c0933 在 Linux/Windows × Python 3.10/3.12 四组全部通过：完整测试、十项变异检查和打包均成功。两组 Linux 的真实 bash run.sh 启动及 v7-jd 健康检查通过。[CI 执行记录](https://github.com/DeepSleepJD/2026-CloudCore-Software-Competition/actions/runs/35337038350)。
